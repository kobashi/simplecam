# Changelog

SimpleCamの主な変更をリリース単位で記録します。

## Unreleased

### Changed

- `AUDIO`開閉ボタンを廃止し、音響コントロールを通常UIとして常時表示。
- 画面上部の操作ボタンを `UI ON/OFF` ボタンと同じサイズに統一。

### Added

- 従来音量から10倍まで調整する `GAIN` スライダーと、輝度に対する音量カーブを指数0.65から1.35まで調整する `CURVE` スライダー。
- `POLY`適用後の最大12音をバー表示する、15fps上限の軽量モニター。

### Fixed

- 高ゲイン時のクリップノイズを抑えるため、コンプレッサーを広いソフトニーと高速アタックへ調整。ソフトクリップのオーバーサンプリングを停止して処理負荷を抑制。
- FM変調量の画素占有率カーブと変調指数を調整し、SINとの差が分かる倍音特性へ変更。
- エンベロープ再トリガー時の音量カーブを現在値から接続し、保持API非対応ブラウザでも進行中のランプを維持。
- 周波数・FM量の更新を平滑化し、再トリガーを最短80ms間隔に制限。閾値0でも変化のないフレームでは再発音しないよう調整。
- 音響解析領域の角ハンドルが画面端の操作UIと重なるとドラッグできない問題を修正。

## v1.4.0 - 2026-09-06

### Added

- 最大同時発音数を1から12音で制限する `POLY` スライダー。
- RGBプレーンの低音・中央・高音割り当てをローテーションする `RGB` ボタン。
- 軽量な `SIN` / `TRI` / `FM` 音色を切り替える `TONE` ボタン。

### Changed

- `SND` / `ENV` / `THRESH` / `REL` / `OCT` と新しい音響設定を、折りたたみ式の `AUDIO` パネルへ集約。
- 発音開始時のアタックを緩やかにし、音響出力へDC除去、コンプレッサー、ソフトクリップ、出力ヘッドルームを追加。

### Fixed

- 多音発音時とSND停止時に発生するクリップノイズを抑制。

## v1.3.0 - 2026-09-06

### Added

- `MIR`ボタンによる左右反転表示。
- 左上・右下ハンドルで変更できる音響解析用の矩形領域。
- `ECO OFF` / `ECO ON` / `ECO MAX`の3段階エコノミーモード。
- 発音トリガー閾値、リリース時間、オクターブシフトの音響コントロール。
- エンベロープ有効・無効の切り替え。

### Changed

- 音量、音程、倍音、発音トリガーを、画面全域ではなく指定矩形内の描画結果から解析。
- ECOレベルに応じて描画解像度、描画FPS、カメラ要求設定、音響解析頻度を段階的に削減。
- カメラフレーム通知に同期してWebGLを描画し、不要なフレーム処理を削減。
- DELAYリングバッファを低解像度GPUテクスチャと時刻基準のフレーム選択へ変更。
- TRAILの減衰を経過時間基準に変更し、FPSが異なる場合も効果時間を維持。
- スライダー、ボタン、タッチ判定を拡大し、UI表示切り替えボタンを右上へ移動。

### Fixed

- 音響エンベロープ発音時のクリップノイズを抑制。
- SND OFF時とバックグラウンド移行時に音響処理が継続する問題を修正。
- フルスクリーン解除後に操作UIが消えることがある問題を修正。

## v1.2 - 2026-05-21

- 前後カメラ切り替えを追加。
- 描画済みWebGLフレームを使ったWebAudio FM合成を追加。
- RGBプレーンごとの4音程、3オクターブ、最大12音の生成に対応。

## v1.1 - 2026-05-19

- WebGL ShaderによるCONTRAST、DELAY、TRAILフィルタを追加。
- フィルタ効果量と残像カーブを調整。

## v1.0 - 2026-05-18

- タッチ操作対応の全画面カメラプレビューを公開。
- ピンチズーム、スワイプパン、フルスクリーン切り替えに対応。
